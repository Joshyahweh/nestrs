# mdBook documentation

This folder contains an `mdBook` scaffold for `nestrs`.

## Build locally

Install mdBook:

```bash
cargo install mdbook
```

Build:

```bash
mdbook build docs
```

Serve (with live reload):

```bash
mdbook serve docs
```

