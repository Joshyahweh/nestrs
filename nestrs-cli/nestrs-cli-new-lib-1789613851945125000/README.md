# nestrs-cli-new-lib-1789613851945125000

Generated with `nestrs-cli new lib`.

A reusable nestrs library crate — controllers, services, and DTOs that
plug into a host app's `AppModule`.

## Development

```bash
cargo build
cargo test
```
